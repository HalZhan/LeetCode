#include "vector"

using namespace std;

class Solution {
public:
    int profitableSchemes(int G, int P, vector<int>& group, vector<int>& profit) {
        const int length = group.size();
        int count = 0;

        return 0;
    }
};